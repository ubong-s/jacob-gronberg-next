// my-theme.ts
import { DefaultTheme } from "styled-components";
import { roundings, colors, sizes, typography } from "./variables";

const myTheme: DefaultTheme = {
  roundings,
  sizes,
  colors,
  typography,
};

export { myTheme };
